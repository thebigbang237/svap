NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
ADMIN_NOTIFICATION_EMAIL=
# Verified Resend sender address. Falls back to onboarding@resend.dev
# (Resend's shared test sender) if unset — fine for local dev, not for
# production sends to real applicants.
RESEND_FROM_EMAIL=
# Absolute site URL, used to build the admin notification email's link to
# the candidature detail page. Falls back to http://localhost:3000 if unset.
NEXT_PUBLIC_SITE_URL=
